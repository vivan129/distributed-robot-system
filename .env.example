# Environment Variables Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to Git (it's in .gitignore)

# Google Gemini API Key
# Get your key at: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: OpenAI API Key (if using GPT models)
# OPENAI_API_KEY=your_openai_key_here

# Optional: Custom PC Server Settings (override config file)
# PC_IP=192.168.1.100
# PC_PORT=5000

# Optional: Raspberry Pi Settings (override config file)
# PI_IP=192.168.1.101
# PI_DISPLAY_PORT=8080

# Flask Secret Key (for web sessions)
FLASK_SECRET_KEY=change_this_to_random_string

# Logging Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Database (for future features)
# DATABASE_URL=sqlite:///robot.db
